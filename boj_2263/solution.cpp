#include <bits/stdc++.h> 
using namespace std;

int main()
{
	int N;
	scanf("%d", &N);
	
	vector<int> in(N);
	for (int i = 0; i < N; ++i) {
		scanf("%d", &in[i]);
	}

	vector<int> post(N);
	for (int i = 0; i < N; ++i) {
		scanf("%d", &post[i]);
	}
	
	

	return 0;
}
